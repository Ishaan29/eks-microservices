apiVersion: v1
kind: Service
metadata:
  name: grafana-proxy
  namespace: dev
spec:
  type: ExternalName
  externalName: monitoring-grafana.monitoring.svc.cluster.local
  ports:
  - port: 80
    targetPort: 80